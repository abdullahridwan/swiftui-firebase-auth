
# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'swiftui-firebase-auth (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swiftui-firebase-auth (iOS)
pod "Firebase/Core"
pod "Firebase/Auth"

end

target 'swiftui-firebase-auth (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for swiftui-firebase-auth (macOS)

end
